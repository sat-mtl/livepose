export SCORE_AUDIO_BACKEND=dummy
export SCORE_DISABLE_LIBRARY=1
export SCORE_DISABLE_AUDIO_PLUGINS=1

# Uncomment to reveal the NDI / Spout / Syphon inputs in the Input Source picker
# (platform-filtered: Spout on Windows, Syphon on macOS, NDI everywhere).
# export LIVEPOSE_ADVANCED_IO=1
